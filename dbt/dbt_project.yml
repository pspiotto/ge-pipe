name: ge_pipe
version: "1.0.0"
config-version: 2

profile: ge_pipe

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ge_pipe:
    staging:
      +schema: stg
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
