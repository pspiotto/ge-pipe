ge_pipe:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'gepipe') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'gepipe') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'ge_pipe') }}"
      schema: public
      threads: 4
